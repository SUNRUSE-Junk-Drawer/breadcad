matrix:
  include:
    - os: linux
      language: c
      compiler: gcc
      script:
        - make
    - os: linux
      language: c
      compiler: clang
      script:
        - make
    - os: windows
      language: c
      install:
        - choco install --limit-output --no-progress --yes make
      script:
        - make
    - os: osx
      language: c
      script:
        - make
