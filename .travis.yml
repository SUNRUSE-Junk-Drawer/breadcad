matrix:
  include:
    - os: linux
      language: c
      compiler: gcc
      script:
        - bash setup.bash 'bin/' make
    - os: linux
      language: c
      compiler: clang
      script:
        - bash setup.bash 'bin/' make
    - os: windows
      language: c
      script:
        - bash setup.bash 'bin/' mingw32-make
    - os: osx
      language: c
      script:
        - bash setup.bash 'bin/' make
